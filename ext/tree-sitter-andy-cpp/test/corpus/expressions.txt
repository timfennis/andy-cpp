==================
list comprehension with guard
==================

[x * 2 for x in 0..10, if x != 3]

---

(source_file
  (list_comprehension
    body: (binary_expression
      left: (identifier)
      right: (integer))
    (comprehension_clauses
      (for_binding
        pattern: (identifier)
        sequence: (range_expression
          start: (integer)
          end: (integer)))
      (if_guard
        condition: (binary_expression
          left: (identifier)
          right: (integer))))))

==================
not in operator
==================

assert(8 not in nums);

---

(source_file
  (call
    function: (identifier)
    arguments: (arguments
      (binary_expression
        left: (integer)
        right: (identifier)))))

==================
destructuring for loop
==================

for key, value in map { print(key); }

---

(source_file
  (for_expression
    (for_binding
      pattern: (pattern_sequence
        (identifier)
        (identifier))
      sequence: (identifier))
    body: (block
      (call
        function: (identifier)
        arguments: (arguments
          (identifier))))))

==================
method chaining and indexing
==================

xs.map(f)[0].len();

---

(source_file
  (member_expression
    object: (index
      value: (member_expression
        object: (identifier)
        method: (identifier)
        arguments: (arguments
          (identifier)))
      index: (integer))
    method: (identifier)
    arguments: (arguments)))

==================
exponent is right associative
==================

2 ^ 3 ^ 2;

---

(source_file
  (binary_expression
    left: (integer)
    right: (binary_expression
      left: (integer)
      right: (integer))))
