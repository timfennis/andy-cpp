// Test -= operator on sets and maps

let set = %{1, 2, 3, 4};
set -= %{2, 3};
assert_eq(set.len(), 2);
assert(set[1] == ());
assert(set[4] == ());

let map = %{"a": 1, "b": 2, "c": 3};
map -= %{"a": 0, "c": 0};
assert_eq(map.len(), 1);
assert_eq(map["b"], 2);
