--PROGRAM--
let square = fn(x) => x * x;
assert_eq(square(5), 25);

// Assert that this expression produces a tuple
let tuple = fn(x) => x, 2;
assert_eq(tuple.len(), 2);
print(tuple);
--EXPECT--
(function,2)
