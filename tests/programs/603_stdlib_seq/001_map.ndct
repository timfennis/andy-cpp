--PROGRAM--
l := [1,2,3];
fn add_one(n) { n + 1 }
assert_eq(l.map(add_one), [2,3,4]);

s := "foo";
fn ap(n) { "[" <> n <> "]" }
assert_eq(s.map(ap), ["[f]", "[o]", "[o]"]);

print("ok");
--EXPECT--
ok
