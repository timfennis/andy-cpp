foo := 3 * (2 + 5);
print(foo, str(foo) + " wow so good");

while foo > true {
    // creates a new variable bar
    bar := if 3 > 2 { 6 } else { 5 };
    foo = bar;
}

fn do_something() {
    return "does nothing";
}
