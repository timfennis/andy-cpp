// expect-error: No function called 'dist' found that matches the arguments 'Other'
struct Point { x: Int, y: Int }
struct Other { x: Int, y: Int }

fn dist(p: Point) => p.x

print(dist(Other(1, 2)))
