// `abs(i64::MIN)` and `-(i64::MIN)` overflow the i64 range because the
// positive magnitude `2^63` does not fit in an i64. Int arithmetic reports
// that checked overflow rather than promoting implicitly.
// expect-error: integer absolute value overflowed
print(abs(-9223372036854775807 - 1));
