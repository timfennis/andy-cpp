// Formatting the "cannot compare" error walked the self-referential list
// forever and aborted the process with a stack overflow. The bounded
// description stops at a fixed depth instead.
// expect-error: cannot compare
let a = [];
a.push(a);
print(a < 1);
