// expect-error: No function called '+' found that matches the arguments
// Per-position vec resolution catches this at compile time: position 2 has
// nested tuples and `+` has no overload that accepts `Tuple<Int>` operands.
(1,1,(1,)) + (1,1,(1,))
