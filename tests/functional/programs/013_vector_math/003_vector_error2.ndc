// expect-error: No function called '+' found that matches the arguments
// This looks valid, but isn't — position 2 has nested tuples and `+` has
// no overload that accepts `Tuple<Int>`. Per-position resolution catches
// this at compile time now.
(1,1,(1,)) + (1,1,(1,))
