// randi accepts integer Number bounds in any combination with Int
for _ in 0..100 {
    let x: Int = randi(10n);
    assert(x >= 0 and x < 10);
}

for _ in 0..100 {
    let x: Int = randi(-5n, 5n);
    assert(x >= -5 and x < 5);
}

assert_eq(randi(5n, 6), 5);
assert_eq(randi(5, 6n), 5);
